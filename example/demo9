a=2147483646
echo "a1 = $a"      # a = 2147483646
let "a+=1"          # 把变量"a"的值自增一。
echo "a2 = $a"      # a = 2147483647
let "a+=1000000000" # 再自增"a"一次,超过这个限制。

# 到2.05b版本为止，Bash支持64位的整数。
echo "a3 = $a"      # a = -2147483648
                    #      错误 (溢出)
                    # 新的版本中不再错误